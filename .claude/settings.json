{
  "//": "Merge into .claude/settings.json. Wires the db-safety hook AND pre-approves the tools that write-capable subagents need (subagents can't show permission prompts; an un-approved call is treated as DENIED).",
  "hooks": {
    "PreToolUse": []
  },
  "permissions": {
    "//comment": "Pre-approve the qa-engineer's Playwright + planner's Brave MCP calls and test-dir writes so they don't silently deny inside the subagent. Tools are served by the `MCP_DOCKER` gateway (docker mcp gateway run), so the namespace is mcp__MCP_DOCKER__* — verified via `docker mcp tools list` 2026-06-18. Note: the gateway has no browser_generate_playwright_test. Tighten the Write globs to your real test dirs.",
    "allow": [
      "mcp__MCP_DOCKER__browser_navigate",
      "mcp__MCP_DOCKER__browser_click",
      "mcp__MCP_DOCKER__browser_type",
      "mcp__MCP_DOCKER__browser_snapshot",
      "mcp__MCP_DOCKER__browser_take_screenshot",
      "mcp__MCP_DOCKER__browser_wait_for",
      "mcp__MCP_DOCKER__browser_evaluate",
      "mcp__MCP_DOCKER__browser_console_messages",
      "mcp__MCP_DOCKER__brave_web_search",
      "mcp__MCP_DOCKER__brave_local_search",
      "Write(e2e/**)",
      "Edit(e2e/**)"
    ]
  },
  "env": {
    "//comment": "Uncomment to enable experimental agent-teams mode (teammates message each other). Requires Claude Code v2.1.32+.",
    "// CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS": "1"
  }
}
